# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
*.bak

# Node / JS test deps (lockfile is intentionally tracked for `npm ci`)
node_modules/
coverage/
.nyc_output/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Browser extension build / packaging artifacts
*.zip
*.crx
*.pem
dist/
build/
web-ext-artifacts/

# Logs (the native host writes /tmp/vault-clipper.log — outside the repo,
# but we keep this glob in case contributors redirect it locally)
*.log

# Local environment / secrets
.env
.env.local
.env.*.local

# Release workflow scratch (generated by .github/workflows/release.yml)
RELEASE_NOTES.md
